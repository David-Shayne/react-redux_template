const initialState = {
    prop:
        "You have successfully installed David Shayne's react-redux template.\nHappy Hacking!"
};

const primaryReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default primaryReducer;
